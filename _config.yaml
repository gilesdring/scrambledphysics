title: ScrambledPhysics
description: |
  Simple physics library for the Processing language, written in pure
  processing, in a single file.
baseurl: /scrambledphysics

theme: jekyll-theme-tactile

sass:
    style: compressed
